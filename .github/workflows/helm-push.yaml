name: Build and Push Helm Chart to GHCR

on:
  push:
    branches: [main]
    paths:
      - 'Chart.yaml'
      - 'values.yaml'
      - 'templates/**'
      - '.github/workflows/helm-push.yaml'

permissions:
  packages: write
  contents: read

jobs:
  push-helm-chart:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: v3.13.0

      - name: Log in to GHCR
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ghcr.io -u ${{ github.actor }} --password-stdin

      - name: Package Helm chart
        run: |
          mkdir -p packaged
          helm dependency update .
          helm package . --destination packaged

      - name: Push Helm chart to GHCR
        run: |
          helm push packaged/*.tgz oci://ghcr.io/imranahmad88pr147/charts
